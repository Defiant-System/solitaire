
let Test = {
	init() {

		// setTimeout(() => window.find(`.toolbar-tool_[data-arg="freecell"]`).trigger("click"), 100);

		return;
		
		// show alert dialog
		setTimeout(() => window.dialog.alert("Testing alert funcionality. Can't autocomplete more&hellip;"), 300);
	}
};
